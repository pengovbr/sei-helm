apiVersion: v1
kind: Service
metadata:
  name: ldap
  labels:
    {{- include "sei-app.labels" . | nindent 4 }}
spec:
  externalName: {{ .Values.app.servicoExterno.ldap }}
  sessionAffinity: None
  type: ExternalName
