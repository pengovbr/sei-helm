apiVersion: v1
kind: Service
metadata:
  name: solr
  labels:
    {{- include "sei-app.labels" . | nindent 4 }}
spec:
  externalName: solr.{{ .Values.app.solrnamespace }}.svc.cluster.local.
  sessionAffinity: None
  type: ExternalName
