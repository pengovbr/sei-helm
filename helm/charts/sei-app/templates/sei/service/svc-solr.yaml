apiVersion: v1
kind: Service
metadata:
  name: solr
  labels:
    {{- include "sei-app.labels" . | nindent 4 }}
spec:
  externalName: {{ .Values.app.servicoExterno.solr }}
  sessionAffinity: None
  type: ExternalName
