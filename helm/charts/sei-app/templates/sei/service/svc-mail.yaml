apiVersion: v1
kind: Service
metadata:
  name: mail
  labels:
    {{- include "sei-app.labels" . | nindent 4 }}
spec:
  externalName: {{ .Values.app.servicoExterno.mail }}
  sessionAffinity: None
  type: ExternalName
