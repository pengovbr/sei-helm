apiVersion: v1
kind: Service
metadata:
  name: db
  labels:
    {{- include "sei-app.labels" . | nindent 4 }}
spec:
  externalName: {{ .Values.app.servicoExterno.db }}
  sessionAffinity: None
  type: ExternalName