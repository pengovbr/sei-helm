apiVersion: v1
kind: Service
metadata:
  name: solr
  labels:
    {{- include "sei-solr.labels" . | nindent 4 }}
spec:
  clusterIP: None
  ports:
  - name: default
    port: 8983
    protocol: TCP
    targetPort: 8983
  selector:
    servico: solr