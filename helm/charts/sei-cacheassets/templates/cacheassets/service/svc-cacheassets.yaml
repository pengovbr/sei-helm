apiVersion: v1
kind: Service
metadata:
  name: cacheassets
  labels:
    {{- include "sei-cacheassets.labels" . | nindent 4 }}
spec:
  ports:
  - name: tcp
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    servico: cacheassets
  sessionAffinity: None
  type: ClusterIP